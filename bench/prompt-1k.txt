You are a senior systems engineer. Write a precise, technically dense design review of a three-node distributed key-value store that uses Raft for consensus, LSM-tree storage on NVMe, and a gRPC-based front tier. Cover the following in order: (1) write path durability — how a client mutation flows through the gRPC front tier into the Raft log, when the leader acknowledges the client relative to fsync on the log segment, and how group commit amortizes fsync cost across concurrent writers; (2) read path consistency — linearizable reads via ReadIndex versus lease-based reads, the clock-skew assumptions leases require, and why a bounded-drift clock oracle changes the failure envelope; (3) compaction policy — leveled versus tiered compaction for a write-heavy workload with 4 KiB records and 90/10 read/write mix, write amplification targets, and how tombstone garbage collection interacts with snapshot-based Raft catch-up; (4) failure handling — what happens when a follower lags behind the log prefix the leader has already truncated, how InstallSnapshot throttling avoids saturating the 25 GbE replication link, and how the front tier reacts to a leader handover mid-request; (5) capacity math — given 3 nodes, 64 GiB RAM each, 4 TiB NVMe each, a 2 TiB working set with 60% hot 5%, choose block cache, memtable, and page cache budgets, and justify them against the measured read amplification of the LSM shape; (6) observability — the five metrics you would alert on first, with thresholds and why each catches a distinct failure mode that the others miss. Be concrete: name algorithms, give numeric budgets, and state trade-offs explicitly rather than hedging. Where a choice is genuinely debatable, say which way you would decide and what evidence would change your mind.
