# Attempt — <short name>

Status: measured | compatibility-only | static-fit-only | failed | not-attempted
Date: YYYY-MM-DD

## Checkpoint

- Repository + exact revision:
- Exact download bytes (from HF API blob sizes):
- Installed size:
- Quantization (format, group size, sym/asym, packed/unpacked):
- License:
- Source of each number above: measured / community-reported / inferred

## Runtime

- Engine + exact version, commit, or image digest:
- SM80 support: yes / no / untested, with primary-source link:
- Topology used (TP/PP, card count):
- Source: measured / inferred / community-reported

## Static fit calculation

Show the arithmetic. State card count, per-card VRAM, and what is excluded
(CUDA context, activations, KV cache).

## Execution status and outcome

What actually happened. For failures: the exact error, stage, and what was
tested. Do not generalize from a single failure.

## Blocker

The single binding blocker, if any.

## Evidence

Links to redacted raw outputs, upstream issues/PRs, or logs. Every claim above
should trace to something here or be explicitly labeled inferred/untested.

## Re-run instructions

Copy-pasteable commands, pinned revisions, and abort conditions, written so a
future maintainer can reproduce without access to private infrastructure.
